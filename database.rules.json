{
  "rules": {
    "users": {
      "$user": {
        ".read" : "$user == auth.uid",
        ".write": "$user == auth.uid"
      }
    },
    "latest_feeds": {
      ".read" : "false",
      ".write": "false"
    }
  }
}
